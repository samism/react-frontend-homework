import React from 'react';

import './CouldntFetchListings.style.scss';

const CouldntFetchListings = () => (
  <section className="couldnt-fetch">
    <h2>Rats!</h2>
    <h4>
      Looks like we weren&apos;t able to load any listings. Please try again
      later.
    </h4>
  </section>
);

export default CouldntFetchListings;
